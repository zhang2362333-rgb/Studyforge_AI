<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyforge.interaction.mapper.PostFavoriteMapper">
    <select id="countByPostAndUser" resultType="int">
        SELECT COUNT(*)
        FROM post_favorites
        WHERE post_id = #{postId}
          AND user_id = #{userId}
    </select>

    <insert id="insertIgnore">
        INSERT IGNORE INTO post_favorites (
            post_id,
            user_id
        )
        VALUES (
            #{postId},
            #{userId}
        )
    </insert>

    <delete id="deleteByPostAndUser">
        DELETE FROM post_favorites
        WHERE post_id = #{postId}
          AND user_id = #{userId}
    </delete>
</mapper>
