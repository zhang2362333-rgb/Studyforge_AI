<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyforge.system.mapper.UserExperienceMapper">
    <insert id="insertIgnore">
        INSERT IGNORE INTO user_experience_logs (
            user_id,
            action_type,
            experience_delta,
            source_id,
            created_date
        )
        VALUES (
            #{userId},
            #{actionType},
            #{experienceDelta},
            #{sourceId},
            #{createdDate}
        )
    </insert>
</mapper>
